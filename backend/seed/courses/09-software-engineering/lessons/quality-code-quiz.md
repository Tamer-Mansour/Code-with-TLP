# Quiz: Writing Quality Code

**Q1. What is a "code smell"?**
- [ ] Code that runs slowly due to inefficient algorithms
- [x] A surface symptom that suggests a deeper structural or design problem in the code
- [ ] Code that lacks any test coverage
- [ ] A security vulnerability in user input handling

**Q2. The Single Responsibility Principle (SRP) applied to functions means:**
- [x] Each function should do exactly one thing and do it well
- [ ] A function should only be called once throughout the program
- [ ] A function must be fewer than 10 lines long
- [ ] Functions should not call other functions

**Q3. When is it appropriate to add a comment to your code?**
- [ ] To explain what every line of code does
- [ ] To justify every variable name choice
- [x] To explain WHY a non-obvious decision was made, or to warn about a surprising side effect
- [ ] Whenever a function is longer than 5 lines

**Q4. The "N+1 query problem" in a web application refers to:**
- [ ] Writing a query that joins N tables at once
- [x] Fetching a list with one query then executing one additional query per item in the list
- [ ] A query that takes N+1 milliseconds to execute
- [ ] Using N+1 indexes on the same database table

**Q5. Which refactoring technique replaces a scattered code block with a newly named function?**
- [ ] Rename Variable
- [x] Extract Function
- [ ] Introduce Parameter Object
- [ ] Replace Magic Number with Constant

**Q6. In the Strategy design pattern, the main benefit is:**
- [ ] Ensuring only one instance of a class exists
- [ ] Automatically retrying failed operations
- [x] Allowing algorithms or behaviours to be swapped at runtime without changing the code that uses them
- [ ] Recording all method calls for later verification
