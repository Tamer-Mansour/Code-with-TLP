# Word Frequency Counter

Count how many times each unique word appears in a line of text and print the results sorted alphabetically, one word and count per line.

This simulates a classic functional-programming pattern: splitting a string into tokens, building a frequency map, and formatting the output — the same operations you would use with JavaScript's `Map`, `split`, and `sort`.

Read the EXERCISE tab for the full problem statement and examples.
