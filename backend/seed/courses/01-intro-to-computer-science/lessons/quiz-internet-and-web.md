# Quiz: The Internet & The Web

**Q1. What is the purpose of breaking data into packets?**
- [ ] To encrypt data before sending it
- [ ] To make files smaller on disk
- [x] To allow multiple devices to share network links and enable efficient retransmission of only the lost chunks
- [ ] To convert data from binary to text for transmission

**Q2. Which protocol translates a domain name like `openai.com` into an IP address?**
- [ ] HTTP
- [ ] TCP
- [x] DNS
- [ ] IP

**Q3. How many bits does an IPv4 address use?**
- [ ] 16
- [x] 32
- [ ] 64
- [ ] 128

**Q4. Which HTTP status code means "Not Found"?**
- [ ] 200
- [ ] 301
- [ ] 403
- [x] 404

**Q5. What is the difference between HTTP and HTTPS?**
- [ ] HTTPS is faster than HTTP because it compresses data
- [ ] HTTPS uses a different port but sends the same unencrypted data
- [x] HTTPS encrypts HTTP traffic using TLS, providing authentication and protection from eavesdropping
- [ ] HTTPS is only for downloading files, not for interactive web pages

**Q6. Which TCP/IP layer is responsible for addressing and routing packets across networks?**
- [ ] Application layer
- [ ] Transport layer
- [x] Internet layer (IP)
- [ ] Link layer

**Q7. What is the key difference between TCP and UDP?**
- [ ] TCP is faster; UDP is slower
- [x] TCP guarantees reliable, ordered delivery; UDP is faster but offers no delivery or ordering guarantees
- [ ] TCP is used for web; UDP cannot be used for anything important
- [ ] UDP supports encryption; TCP does not

**Q8. You receive HTTP status code 401 when accessing an API. This most likely means:**
- [ ] The URL you requested does not exist
- [ ] The server encountered an internal error
- [x] You need to authenticate (provide credentials) before accessing this resource
- [ ] You have exceeded the rate limit
