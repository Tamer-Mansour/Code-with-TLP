# Quiz: React Basics

**Q1. Component names must:**
- [ ] Be all lowercase
- [x] Start with an uppercase letter
- [ ] Be prefixed with `React.`
- [ ] Be in camelCase

**Q2. The right way to give each item a stable identity in a list is:**
- [ ] Use array index
- [x] Provide a stable, unique `key` prop
- [ ] Use the order of `useEffect`
- [ ] React figures it out automatically

**Q3. Inside a component, props are:**
- [x] Read-only inputs
- [ ] Two-way bindings
- [ ] Mutable internal state
- [ ] Passed via context only

**Q4. JSX `class` attribute is written as:**
- [ ] `class`
- [x] `className`
- [ ] `cls`
- [ ] `tag-class`

**Q5. To render text conditionally, the idiomatic pattern is:**
- [ ] `if (x) { return ... }` inside JSX
- [x] `{x && <Component />}` or ternary inside JSX
- [ ] `<If condition={x}>...</If>`
- [ ] `useConditional`

**Q6. The `children` prop holds:**
- [ ] An array of component metadata
- [x] Whatever is between the component's open and close tags
- [ ] The DOM tree below
- [ ] The HTML output as a string
