# Exercise: Gradient Descent Step

Practice applying one step of gradient descent on a simple linear model, reinforcing how weights update toward minimizing loss.

Given a linear model `y_hat = w * x + b`, a training example, and a learning rate, compute the updated weights after one step of gradient descent.
