# Exercise: Detect Cycle in Undirected Graph

Given an undirected graph, determine whether it contains a cycle using Union-Find (Disjoint Set Union).

This exercise demonstrates the classic Union-Find application: if adding an edge connects two vertices that are already in the same component, a cycle is detected.

See the prompt for the full problem statement and input format.
