# Exercise: Count Connected Components

Count the number of connected components in an undirected graph using DFS or Union-Find.

This exercise builds the pattern of running a graph traversal from every unvisited vertex and counting how many times a fresh traversal is needed.

See the prompt for the full problem statement and input format.
