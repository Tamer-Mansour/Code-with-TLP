# Exercise: Queue via Two Stacks

Implement a FIFO queue using only two stacks (Python lists with only `append` and `pop`).

This exercise demonstrates how two LIFO structures can be composed to yield FIFO semantics, and why the amortised cost of dequeue is O(1).

See the prompt for the full problem statement and input format.
