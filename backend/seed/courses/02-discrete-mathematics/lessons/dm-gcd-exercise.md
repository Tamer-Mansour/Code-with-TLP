# Exercise: Euclid's GCD

Implement the Euclidean algorithm to compute the greatest common divisor of two positive integers.

Read two positive integers from standard input. Output `gcd(a, b)`.

This classic algorithm runs in `O(log(min(a, b)))` steps and is the foundation for modular inverse computation used in RSA cryptography.
