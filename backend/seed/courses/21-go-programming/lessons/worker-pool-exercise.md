# Worker Pool Sum

A typical Go pattern is the worker pool — fan-out work to N goroutines, fan-in results. In this exercise (simulated in Python), you'll just compute the sum of a list of integers, mirroring what each worker would push into a results channel.

See the prompt for the exact contract.
